# Redux Toolkit User Management App

Навчальний проект для демонстрації управління станом за допомогою Redux Toolkit.

## 📝 Опис проекту

Це React-додаток для управління списком користувачів, який використовує **Redux Toolkit** для централізованого управління станом. Проект демонструє сучасний підхід до роботи зі станом у React-додатках, використовуючи найкращі практики Redux.

Додаток дозволяє:
- Переглядати список користувачів
- Вибирати користувача кліком
- Відображати детальну інформацію про обраного користувача

## 🎯 Мета проекту

- Вивчити роботу з **Redux Toolkit** (`createSlice`, `configureStore`)
- Освоїти хуки **React-Redux** (`useSelector`, `useDispatch`)
- Зрозуміти переваги централізованого управління станом
- Навчитися створювати слайси (slices) з редюсерами та діями
- Практикувати роботу з селекторами для читання стану
- Порівняти підходи Context API vs Redux

## 🏗️ Структура проекту

```
my-context-app/
├── src/
│   ├── redux/
│   │   ├── store.js                # Конфігурація Redux store
│   │   └── slices/
│   │       └── usersSlice.js       # Slice користувачів
│   ├── components/
│   │   ├── UserList.jsx            # Список користувачів
│   │   └── UserProfile.jsx         # Профіль користувача
│   ├── App.jsx                     # Головний компонент
│   ├── main.jsx                    # Точка входу + Redux Provider
│   └── App.css                     # Стилі
├── REDUX_REFACTORING.md            # Документація рефакторингу
└── package.json
```

## 🚀 Функціонал

- **Список користувачів**: Відображення списку з 3 користувачів з Redux store
- **Вибір користувача**: Клік по імені відправляє дію `selectUser` через dispatch
- **Профіль**: Показ детальної інформації через селектор `selectSelectedUser`
- **Redux DevTools**: Підтримка Redux DevTools для відлагодження стану

## 💻 Технології

- **React 19** - UI бібліотека
- **Vite** - швидкий збірник
- **Redux Toolkit** - сучасне управління станом
- **React-Redux** - інтеграція Redux з React
- **CSS** - стилізація

## 🔧 Redux Архітектура

### Store
Централізоване сховище стану програми, створене за допомогою `configureStore`.

### Slice (usersSlice)
**Стан:**
```javascript
{
  users: Array<{id, name, email}>,  // Список користувачів
  selectedUser: Object | null        // Обраний користувач
}
```

**Дії (Actions):**
- `selectUser(user)` - вибрати користувача
- `clearSelectedUser()` - скинути вибір

**Селектори:**
- `selectUsers` - отримати всіх користувачів
- `selectSelectedUser` - отримати обраного користувача

## 📦 Встановлення та запуск

1. Клонуйте репозиторій або завантажте файли

2. Встановіть залежності:
```bash
npm install
```

3. Запустіть проект у режимі розробки:
```bash
npm run dev
```

4. Відкрийте браузер за адресою `http://localhost:5173`

5. Для збірки production версії:
```bash
npm run build
```

## 📚 Що ви дізнаєтесь

### 1. Redux Toolkit
- Створення store за допомогою `configureStore`
- Використання `createSlice` для створення редюсерів та дій
- Автоматична генерація action creators
- Immutable оновлення стану з Immer

### 2. React-Redux
- Підключення store через `Provider`
- Хук `useSelector` для читання стану
- Хук `useDispatch` для відправки дій
- Оптимізація ре-рендерів

### 3. Архітектура додатку
- Організація Redux структури (store, slices)
- Створення селекторів для доступу до стану
- Централізоване управління станом
- Односторонній потік даних

### 4. Порівняння підходів
- Redux vs Context API
- Коли використовувати кожен підхід
- Переваги та недоліки

## 🔑 Ключові файли

### store.js
Налаштовує Redux store:
```javascript
configureStore({
  reducer: {
    users: usersReducer
  }
})
```

### usersSlice.js
Створює slice з:
- Початковим станом (users, selectedUser)
- Редюсерами (selectUser, clearSelectedUser)
- Селекторами для доступу до стану
- Автоматично згенерованими діями

### main.jsx
Підключає Redux Provider:
```jsx
<Provider store={store}>
  <App />
</Provider>
```

### UserList.jsx
Використовує Redux для:
- Отримання користувачів: `useSelector(selectUsers)`
- Отримання обраного користувача: `useSelector(selectSelectedUser)`
- Відправки дій: `dispatch(selectUser(user))`

### UserProfile.jsx
Використовує Redux для:
- Отримання вибраного користувача через селектор
- Відображення даних без prop drilling

## 🎓 Переваги Redux Toolkit

✅ **Менше boilerplate коду** - автоматична генерація actions  
✅ **Вбудований Immer** - immutable оновлення стану  
✅ **Redux DevTools** - потужні інструменти для відлагодження  
✅ **TypeScript підтримка** - готово до типізації  
✅ **Краща структура** - організація коду через slices  
✅ **Middleware** - легко додати async логіку  

## 🔄 Історія проекту

Проект був створений з використанням Context API і згодом відрефакторений на Redux Toolkit. Детальна документація рефакторингу доступна у файлі [REDUX_REFACTORING.md](REDUX_REFACTORING.md).

## 📖 Додаткові ресурси

- [Redux Toolkit документація](https://redux-toolkit.js.org/)
- [React-Redux Hooks](https://react-redux.js.org/api/hooks)
- [Redux Style Guide](https://redux.js.org/style-guide/)
- [Redux DevTools Extension](https://github.com/reduxjs/redux-devtools)

## 🛠️ Можливі покращення

- [ ] Додати можливість редагування користувачів
- [ ] Реалізувати видалення користувачів
- [ ] Додати пошук по користувачам
- [ ] Інтегрувати API для завантаження даних
- [ ] Додати async thunks для асинхронних операцій
- [ ] Підключити Redux DevTools для відлагодження
- [ ] Додати TypeScript для type safety

## 👨‍🎓 Навчальний проект

Цей проект створений для навчальних цілей у рамках курсу з вивчення React та Redux.

Навчальний проект для курсу з React
